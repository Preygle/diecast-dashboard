httpx[http2]>=0.28.1
selectolax>=0.3.27
rapidfuzz>=3.10.1
# fastapi must stay >=0.121 so it works with starlette >=0.49, which
# sse-starlette (used by MCP tooling in shared environments) requires.
fastapi>=0.121
starlette>=0.49.1
uvicorn[standard]>=0.34.0
PyYAML>=6.0.2
playwright>=1.49.1
